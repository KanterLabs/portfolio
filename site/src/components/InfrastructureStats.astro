<section class="infrastructure-stats not-prose" aria-label="Homelab platform at a glance">
  <div>
    <strong>2</strong>
    <span>Proxmox environments</span>
    <p>Home services and remote edge workloads have separate operational roles.</p>
  </div>
  <div>
    <strong>3</strong>
    <span>Network trust zones</span>
    <p>Management, public workloads, and private workloads follow different paths.</p>
  </div>
  <div>
    <strong>1–3</strong>
    <span>Ephemeral CI runners</span>
    <p>One warm runner scales to three concurrent organization jobs.</p>
  </div>
  <div>
    <strong>1 pod</strong>
    <span>Per workflow job</span>
    <p>Runner and Docker state are discarded together after completion.</p>
  </div>
</section>

<style>
  .infrastructure-stats {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 0.75rem;
    margin: 2rem 0 2.5rem;
  }

  .infrastructure-stats > div {
    min-width: 0;
    padding: 1rem;
    border: 1px solid var(--line);
    border-radius: 14px;
    background: var(--panel);
  }

  .infrastructure-stats strong {
    display: block;
    color: #8fb8a3;
    font-family: var(--font-family-mono);
    font-size: 1.7rem;
    line-height: 1;
  }

  .infrastructure-stats span {
    display: block;
    margin-top: 0.55rem;
    color: #e8ede9;
    font-size: 0.83rem;
    font-weight: 700;
    line-height: 1.35;
  }

  .infrastructure-stats p {
    margin: 0.45rem 0 0;
    color: #7c8a81;
    font-size: 0.75rem;
    line-height: 1.5;
  }

  :global([data-theme='light']) .infrastructure-stats strong {
    color: #47725c;
  }

  :global([data-theme='light']) .infrastructure-stats span {
    color: #17201b;
  }

  :global([data-theme='light']) .infrastructure-stats p {
    color: #55625a;
  }

  @media (max-width: 780px) {
    .infrastructure-stats {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 430px) {
    .infrastructure-stats {
      grid-template-columns: 1fr;
    }
  }
</style>
