---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span
  class="inline-flex items-center min-h-8 px-3 py-1.5 rounded-full border border-line bg-[rgba(255,255,255,0.04)] text-text-muted text-[0.8rem] font-medium"
>
  {label}
</span>
