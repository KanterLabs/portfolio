---
title: KanterLabs Homelab Platform
summary: A two-site Proxmox platform with hardened edge VMs, isolated network planes, private DNS and TLS, and ephemeral CI, all governed by reviewed infrastructure configuration.
highlight: Turns a collection of servers into an intentional platform with fail-closed network boundaries and guarded, recoverable operations.
impact: Replaced loosely coupled ingress rules and persistent runners with reusable edge boundaries, default-deny network policy, and organization-wide ephemeral CI.
constraint: Keep the system understandable and recoverable without publishing secrets, mounting host privileges into CI jobs, or pretending every change is continuously reconciled.
role: Infrastructure designer, builder, and operator.
scope: Proxmox, Linux, VLANs, nftables, Caddy, CoreDNS, Cloudflare, Tailscale, private PKI, k3s, Actions Runner Controller, GHCR, and GitHub Actions.
keyOutcome: A reusable two-edge platform whose public, private, management, and origin traffic paths are explicit, tested, and recoverable.
featured: true
order: 1
stack:
  - Proxmox VE
  - Linux
  - Caddy
  - Tailscale
  - k3s
  - GitHub Actions
  - ARC
  - Docker
status: shipped
date: 2026-07-25
socialImage: /og/project-homelab-platform.svg
links:
  - label: View portfolio source
    href: https://github.com/KanterLabs/portfolio
system:
  caption: Public and private traffic enter through distinct Cloudflare and Tailscale paths on hardened edge VMs; default-deny policy isolates management, ingress, and origin planes while CI runs in ephemeral ARC pods.
  tiers:
    - nodes:
        - label: Public entry
          title: Cloudflare
          detail: DNS and public TLS
        - label: Private entry
          title: Tailscale Serve
          detail: Tailnet-only HTTPS
    - nodes:
        - label: Edge boundary
          title: Hardened edges
          detail: Exact, fail-closed routes
          kind: accent
    - nodes:
        - label: Zone
          title: Public workloads
          detail: Portfolio and app platform
        - label: Zone
          title: Private workloads
          detail: Tailnet-only apps
  footer:
    - label: CI
      title: runs-on homelab → ephemeral ARC pod
    - label: Secrets
      title: Outside Git, injected at runtime
---

import Callout from '../../components/Callout.astro';
import DiagramFigure from '../../components/DiagramFigure.astro';
import InfrastructureStats from '../../components/InfrastructureStats.astro';

## Overview

My homelab grew from a development server into a small platform spread across two Proxmox environments. Each site now has a purpose-built edge boundary: the home environment supports development, CI, secrets, and selected origins, while the remote environment carries production application workloads and Internet-facing routing.

The interesting part is not the machine count. It is the set of boundaries around them:

- public traffic enters through dedicated Cloudflare paths instead of reaching workloads directly
- private applications have separate Tailnet-only names and listeners
- management, ingress, and origin networks are distinct planes with default-deny forwarding
- edge services bind to exact interfaces, and private DNS answers only on the intended views
- GitHub Actions jobs run in disposable pods instead of long-lived repository runners
- credentials live outside Git and are injected only where they are needed

<InfrastructureStats />

<DiagramFigure
  src="/diagrams/homelab-estate.svg"
  alt="High-level map of the KanterLabs homelab and Kantercloud environments"
  caption="Both sites use explicit edge boundaries. The home environment provides development and CI plus an isolated edge; the remote environment provides the production edge and separated application zones."
  wide={true}
/>

## How the Infrastructure Repository Fits

The private `KanterLabs/infrastructure` repository is the **reviewed source of truth for selected infrastructure**, not a complete inventory and not a continuously reconciling GitOps controller.

That distinction matters. A merged change records the intended configuration and gives me something reviewable, testable, and recoverable. It does not automatically rewrite every target host.

| Repository area | What it describes | How it reaches runtime |
|---|---|---|
| Edge configuration | Exact Caddy listeners, split-horizon DNS, Tailnet policy, and workload interfaces | Rendered from reviewed plans, validated, and deliberately installed on each edge VM |
| Workload policy | Host firewall rules and private/public service bindings | Applied on the relevant workload |
| Hypervisor networking | VLAN-aware bridges, NAT, forwarding allowlists, and isolation between planes | Installed through guarded host-managed transactions |
| CI platform | k3s, ARC, runner pods, custom images, verification, and dashboard | Installed with versioned scripts and Helm values |
| GitHub workflows | Runner image publication and end-to-end smoke checks | Executed by GitHub Actions |

Secrets are intentionally absent. GitHub App credentials, deployment keys, and service tokens are provisioned through GitHub, Infisical, Kubernetes Secrets, or root-only environment files.

<Callout type="note" title="Reviewed does not mean automatic">
  The repository makes changes inspectable and repeatable, while the install scripts preserve an explicit operator checkpoint. That is a deliberate tradeoff for a small environment where an incorrect firewall or ingress change can disconnect the very host needed to repair it.
</Callout>

<DiagramFigure
  src="/diagrams/repository-control-map.svg"
  alt="Map from infrastructure repository areas to the systems they configure"
  caption="Git records the intended state. Workflows automate safe build and smoke-test paths; host and network changes retain an operator-controlled apply step."
  wide={true}
/>

## Four Ingress Planes, Two Edge Boundaries

The edge VMs own application ingress. Workloads do not receive arbitrary inbound traffic from the Internet, the Tailnet, management networks, or neighboring workload zones. The platform verifies four independent HTTPS planes: remote public, remote private, home public, and home private.

### Public path

At the remote site, public requests pass through Cloudflare, reach the hypervisor's narrow forwarding boundary, and are sent only to the edge VM's public interface. At home, a connector creates outbound transport to Cloudflare without opening unsolicited inbound access. In both cases, Caddy accepts only intended hostnames and proxies to the matching origin.

Origins live behind those boundaries. Their host firewalls permit application traffic from the relevant edge source, not from an entire LAN or VLAN. TLS is re-established and verified on sensitive edge-to-origin paths rather than assuming that internal transport is trusted.

### Private path

Private applications use named Tailscale services or deliberately retained Serve routes as their only entrance. Tailscale hands traffic to loopback-only or explicitly bound Caddy listeners, while split-horizon DNS prevents private names from becoming public discovery paths. Direct Tailnet routing into workload networks is not a substitute for the named service entrance.

### Zone policy

The design separates four concerns:

1. **management** for administration during the migration period
2. **ingress** for edge-facing listeners and transport connectors
3. **public origins** for services intentionally reachable through a public edge path
4. **private origins** for Tailnet-only applications

Forwarding between those planes is denied by default and opened only for named flows. Edge input, forwarding, and output policy fail closed; interfaces and source addresses are matched explicitly. Public and private origins can receive the traffic they need, but they cannot route laterally or use management as an alternate application entrance.

The same policy is exercised negatively: unknown hostnames, incorrect TLS names, cross-plane requests, plaintext origin access, spoofed proxy identity, and direct workload access must fail. Synthetic canaries prove each approved plane without depending on an application migration.

<DiagramFigure
  src="/diagrams/homelab-trust-zones.svg"
  alt="Public, private, and management traffic paths through hardened edge boundaries"
  caption="Every application path crosses a named entrance and exact listener. Default-deny policy keeps management, ingress, public-origin, and private-origin traffic from silently substituting for one another."
  wide={true}
/>

## Organization-Wide Ephemeral CI

The CI migration replaced repository-specific, persistent runners with one organization runner scale set selected by:

```yaml
jobs:
  test:
    runs-on: homelab
```

The runner group is available to current and future KanterLabs repositories. Actions Runner Controller maintains one warm runner and can scale to three concurrent runners when jobs arrive.

Each job receives a fresh Kubernetes pod with two cooperating containers:

- a non-root Actions runner containing the pinned development toolchain
- a private Docker-in-Docker sidecar for image builds and Compose-based tests

The containers share only the job workspace, Docker socket, and bounded ephemeral storage they need. The host Docker socket, host home directory, and Kubernetes service-account token are not mounted into the runner.

When the job finishes, ARC deletes the pod and its private Docker daemon. Package and compiler caches can persist on the runner host, but workspace and container state do not.

<DiagramFigure
  src="/diagrams/runner-lifecycle.svg"
  alt="Lifecycle of a GitHub Actions job on the KanterLabs homelab runner scale set"
  caption="ARC turns an organization workflow request into a disposable runner pod, then removes the job environment after completion."
  wide={true}
/>

## A Pinned Runner Environment

The runner image is built from Git and published to GitHub Container Registry. ARC references the result by immutable digest, so a tag cannot silently change the environment underneath a workflow.

The image carries the common tools used across the organization:

- Node.js and pnpm for web projects
- Rust, Clippy, rustfmt, and sccache for systems projects
- Docker Compose and Buildx for container workflows
- Python and YAML tooling for repository contract checks
- Chromium and Firefox dependencies for browser tests
- GitHub and Infisical CLIs for controlled automation

A smoke workflow verifies both the toolchain and the isolation contract. It confirms the runner is ephemeral and non-root, checks that sensitive host paths are absent, and performs a real Docker build through the per-job daemon.

## Resource and Failure Boundaries

The runner platform uses a single-node k3s cluster because the goal is controlled CI capacity, not a general-purpose Kubernetes environment.

That keeps operations small, but it creates an honest failure domain: if the runner VM or its Proxmox host is unavailable, homelab jobs wait. The configuration accounts for this with:

- reserved CPU, memory, and disk space for the node and Kubernetes services
- explicit pod requests and limits
- bounded work and Docker storage per job
- eviction thresholds before the node exhausts storage or memory
- pinned installer, chart, controller, runner, and Docker image versions
- retained migration state and a documented rollback window

The custom dashboard is LAN-only and read-only. It combines GitHub job state with host and cluster health without sending its server-side credentials to the browser.

## Security Decisions

The platform does not claim that running untrusted CI is risk-free. Docker-in-Docker still needs a privileged sidecar. The security boundary is instead made narrow and disposable:

- privilege is isolated to the job's Docker sidecar rather than the host daemon
- the runner itself stays non-root
- credentials are not baked into the image
- host access is limited to a purpose-built cache directory
- workloads cannot move laterally between public, private, and management zones
- secret values never enter the infrastructure repository

The network follows the same philosophy. Configuration is rendered deterministically, rejected if it contains wildcard binds or unapproved routes, and applied through a guarded transaction. A timed rollback, independent management-path checks, configuration hashes, service health, positive canaries, and negative exposure tests must all succeed before a candidate becomes the retained state.

This is a practical balance for my own organization: enough capability to run browser, Rust, Node, and container-heavy tests, with far less persistent state than the old runner fleet.

## What I Learned

The largest improvement came from treating infrastructure as a set of contracts instead of a collection of machines.

- **Ingress contract:** every application has one intended entrance.
- **Network contract:** every allowed flow names its source, destination, interface, and purpose; everything else fails closed.
- **Name and TLS contract:** public and private DNS views, hostnames, and certificates agree with the traffic plane.
- **CI contract:** `runs-on: homelab` always means the same disposable environment.
- **Repository contract:** reviewed files describe intended state, while secrets and host-local state remain outside Git.
- **Recovery contract:** network changes keep an automatic rollback and an independently tested management path until the replacement has been exercised.

The result is still a homelab—small, resource-conscious, and operated by one person—but it now behaves like a platform I can reason about, review, and extend.
